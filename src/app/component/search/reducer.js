
const initizalize = {
    dataSearch: [],
}

const searchReducer = (state = initizalize, action) => {
    switch (action.type) {
        case 'SEARCH': {
            const value = action.words;
            
        }

    
        default:
            break;
    }
} 